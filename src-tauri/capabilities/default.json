{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Neko desktop capabilities: app commands via invoke plus core defaults (events for neko:// listeners). Windows are created and managed from Rust, so no extra window/webview/plugin permissions are granted to the frontend.",
  "windows": ["settings", "break-*"],
  "permissions": ["core:default"]
}
